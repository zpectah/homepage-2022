import React from 'react';
import { Box } from '@mui/material';

const NotFound = () => (
    <Box
        sx={{
            width: '100%',
            height: '100%',
        }}
    >
        NotFound
    </Box>
);

export default NotFound;
